# Текущее состояние проекта

Документ фиксирует состояние после PR #38. В проверке Pull Request пройдено
**111 тестов**; после слияния та же проверка повторяется на `main`.

## Завершённые этапы

| Этап | Что подтверждено | Основные PR |
|---|---|---|
| 0 — База | Исходное состояние и CI зафиксированы. | #15, #16 |
| 1 — Входные данные | Проверяются схема, значения и пустой канал; есть паспорт данных. | #17, #22, #34 |
| 2 — Безопасный ML | Нет обучения на анализируемом CSV; данные и артефакты разделены и совместимы. | #18–#21 |
| 3 — Время и физика | Интервал и скорость учитывают реальные timestamp. | #24–#26 |
| 4 — Конфигурация | Пороги и ML-схема имеют единые источники истины. | #21, #27 |
| 5 — Оценка | Есть метрики, confusion matrix и задержка обнаружения. | #19, #28, #29 |
| 6 — Объяснимость | Сохраняются все правила, главная причина и число правил. | #30, #31 |
| 7 — События и риск | Score стабилен относительно baseline; точки группируются в события. | #32, #33 |
| 8 — Дашборд | Видны режим, качество данных, события и подробный журнал. | #23, #33, #34 |
| 9 — Документация и защита | Есть архитектура, технический отчёт, презентация и воспроизводимый демо-сценарий. | #35–#37 |

## Как проверять актуальное состояние

```bash
pip install -r requirements.txt -r requirements-dev.txt
python -m pytest -q
python evaluation.py --output evaluation_report.json
streamlit run app.py
```

Перед изменением ML-признаков нужно проверить `model_schema.py`, переобучить
комплект модели и повторить независимую evaluation. Перед изменением правил
нужно обновить `rule_config.py` и регрессионные тесты.

## Оставшаяся дорожная карта

Все обязательные задачи завершены. После фиксации baseline-тега осталось
четыре необязательных направления:

- 2 желательные задачи: матрица риска и карточка объяснения тревоги;
- 2 идеи на будущее: потоковая обработка и возможные Docker/API/БД.

Карточки «отчёт о качестве входных данных» и «таблица событий» закрыты в
PR #34 и #33. Архитектурное описание закрыто в PR #35, итоговый технический
отчёт — в PR #36, презентация и демонстрационный сценарий — в PR #37.
Стабильная точка `v1.0.0` документируется в PR #38 и ставится на проверенный
merge-коммит после успешного CI на `main`.

## Формат записи следующих изменений

После каждой задачи документируйте: проблему, решение, изменённые файлы,
команды проверки, результат, ограничения и новые понятия. Notion остаётся
подробным журналом карточек, а этот файл — компактной картой проверенного
состояния репозитория.
